---
layout: base
title: Listening
permalink: /listening/
---

<section class="section-block">
  <h1 class="section-title">Listening</h1>

  {% if albumwhale and albumwhale.items and albumwhale.items.length %}
    <ul class="site-list-of-posts">
      {% for item in albumwhale.items %}
        <li class="site-list-of-posts-item">
          <div class="site-list-link">
            <a href="{{ item.link }}">{{ item.title }}</a>
          </div>
          {% if item.pubDate %}
            <time class="site-list-date" datetime="{{ item.pubDate | htmlDateString }}">
              {{ item.pubDate | dateDisplay }}
            </time>
          {% endif %}
        </li>
      {% endfor %}
    </ul>
  {% else %}
    <p>No Album Whale entries yet.</p>
  {% endif %}
</section>
